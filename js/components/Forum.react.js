var Forum = React.createClass({
  render: function () {
    return React.createElement(
      'div', //div element
       null, //no attributes
      React.createElement(ForumHeader, null)
     );
  }
});
